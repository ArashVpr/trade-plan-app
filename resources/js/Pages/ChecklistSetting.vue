<template>
  
</template>

<script setup>

// evaluationScore() {
//     let score = 0;
//     const weights = this.settings?.scoring_weights || { zone_qualifiers: 1, technicals: 1, fundamentals: 1 };
//     score += this.checkedZoneQualifiers.filter(Boolean).length * 10 * weights.zone_qualifiers;
//     score += (['Very Cheap', 'Very Expensive'].includes(this.technicals.location) ? 20 : ['Cheap', 'Expensive'].includes(this.technicals.location) ? 10 : 0) * weights.technicals;
//     score += (this.technicals.direction === 'Correction' ? 20 : this.technicals.direction === 'Impulsion' ? 10 : 0) * weights.technicals;
//     score += (['Undervalued', 'Overvalued'].includes(this.fundamentals.valuation) ? 20 : 0) * weights.fundamentals;
//     score += (this.fundamentals.seasonalConfluence === 'Yes' ? 10 : 0) * weights.fundamentals;
//     score += (this.fundamentals.nonCommercials === 'Divergence' ? 20 : 0) * weights.fundamentals;
//     score += (['Bullish', 'Bearish'].includes(this.fundamentals.cotIndex) ? 20 : 0) * weights.fundamentals;
//     return Math.round(score);
// }
</script>

